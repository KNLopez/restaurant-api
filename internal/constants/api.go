package constants

const (
	BaseURL = "/api/v1"
)

// Route groups
const (
	UsersRoute       = BaseURL + "/users"
	RestaurantsRoute = BaseURL + "/restaurants"
	OrdersRoute      = BaseURL + "/orders"
	TablesRoute      = BaseURL + "/tables"
)
